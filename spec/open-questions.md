# Open Questions (living)

- How closely should quick-nav key bindings mirror NVDA/JAWS vs avoid conflicts with web apps?
- Should VC wrap by default when reaching the end of a category?
- How aggressively should we coalesce aria-live announcements?
- How should we represent “grouping” (fieldset/legend, aria-group) in contextText (MVP or later)?
